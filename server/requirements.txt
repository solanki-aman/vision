fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
python-multipart>=0.0.9,<1.0
asyncpg>=0.30,<1.0
pydantic>=2.9,<3.0
httpx>=0.27,<1.0
xai-sdk>=1.17,<2.0
langgraph>=0.2,<2.0
langchain-core>=0.3,<2.0
langchain-xai>=0.1,<2.0
# Floor raised past the streaming-events redaction bypass (CVE-2026-41182). Traces
# carry document page images, so client-side redaction is load-bearing here.
langsmith>=0.7.31,<1.0
# Documents are rasterised, never text-extracted — see app/documents.py. pypdfium2
# is Apache-2.0 and only has to render, so nothing pulls in an AGPL PDF stack.
pypdfium2>=4.30,<6.0
pillow>=10.4,<13.0
minio>=7.2,<8.0
# OIDC id_token verification. joserfc rather than authlib.jose, which authlib
# itself deprecates; the claim checks around it are tested in tests/test_auth.py.
joserfc>=1.0,<2.0
pytest>=8.3,<9.0
pytest-asyncio>=0.24,<1.0
